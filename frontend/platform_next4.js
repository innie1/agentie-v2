(()=>{
  if(window.__agentieNext4UI)return;window.__agentieNext4UI=true;
  const css=document.createElement('style');css.textContent=`
  .platform-chats-launch,.platform-skills-launch,.pa-automation-launch{display:none!important}.n4-launch{width:100%;margin-top:8px;border:1px solid var(--border);background:transparent;color:var(--text);border-radius:10px;padding:8px 10px;text-align:left;cursor:pointer}.n4-launch:hover{background:var(--soft)}
  .n4-modal{position:fixed;inset:0;z-index:15000;background:rgba(0,0,0,.48);display:grid;place-items:center;padding:18px}.n4-panel{width:min(840px,100%);max-height:92vh;overflow:auto;background:var(--panel);border:1px solid var(--border);border-radius:18px;padding:18px;box-shadow:0 26px 78px rgba(0,0,0,.32)}.n4-head{display:flex;gap:12px;align-items:center;justify-content:space-between}.n4-head h3{font-size:17px;margin:0}.n4-close{border:0;background:transparent;color:var(--muted);font-size:23px;cursor:pointer}.n4-help{font-size:10px;color:var(--muted);line-height:1.45;margin-top:5px}.n4-actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:11px}.n4-actions button,.n4-actions select{border:1px solid var(--border);border-radius:9px;background:var(--panel);color:var(--text);padding:7px 10px;font-size:10px;cursor:pointer}.n4-actions .primary{background:#0b84ff;color:#fff;border-color:#0b84ff}.n4-actions button:disabled{opacity:.5;cursor:default}.n4-section{margin-top:16px;padding-top:14px;border-top:1px solid var(--border)}.n4-section-title{font-size:10px;text-transform:uppercase;letter-spacing:.06em;color:var(--muted);margin-bottom:8px}.n4-list{display:grid;gap:8px}.n4-row{border:1px solid var(--border);border-radius:11px;background:var(--soft);padding:10px}.n4-row.clickable{cursor:pointer}.n4-row.clickable:hover{border-color:color-mix(in srgb,#0b84ff 45%,var(--border))}.n4-row-head{display:flex;align-items:flex-start;justify-content:space-between;gap:10px}.n4-row strong,.n4-row small{display:block}.n4-row strong{font-size:12px}.n4-row small{font-size:9px;color:var(--muted);margin-top:3px;line-height:1.4}.n4-field{display:grid;gap:5px;margin-top:10px}.n4-field label{font-size:10px;font-weight:650}.n4-field input,.n4-field textarea,.n4-field select{width:100%;box-sizing:border-box;border:1px solid var(--border);border-radius:9px;background:var(--soft);color:var(--text);padding:9px 10px;outline:0}.n4-field textarea{min-height:74px;resize:vertical}.n4-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.n4-agent-picks{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:7px;margin-top:10px}.n4-agent-pick{display:flex;gap:8px;align-items:center;padding:8px;border:1px solid var(--border);border-radius:10px;background:var(--soft);cursor:pointer}.n4-orb{width:29px;height:29px;border-radius:50%;display:grid;place-items:center;font-size:8px;font-weight:800;color:#111;flex:none}.n4-agent-pick span{min-width:0}.n4-agent-pick strong,.n4-agent-pick small{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.n4-agent-pick strong{font-size:11px}.n4-agent-pick small{font-size:9px;color:var(--muted)}.n4-error{font-size:10px;color:#ef4444;margin-top:8px}.n4-success{font-size:10px;color:#22a06b;margin-top:8px}
  .n4-presence{display:flex;gap:7px;flex-wrap:wrap;margin-top:9px}.n4-presence-chip{display:flex;align-items:center;gap:5px;padding:4px 7px;border:1px solid var(--border);border-radius:999px;background:var(--soft);font-size:9px}.n4-dot{width:7px;height:7px;border-radius:50%;background:#9ca3af}.n4-dot.working{background:#22c55e}.n4-dot.queued{background:#f59e0b}.n4-dot.done{background:#3b82f6}.n4-dot.failed{background:#ef4444}.n4-msg{border:1px solid var(--border);border-radius:11px;background:var(--soft);padding:9px 10px;margin-top:8px}.n4-msg.reply{margin-left:22px}.n4-msg-meta{display:flex;gap:7px;flex-wrap:wrap;font-size:9px;color:var(--muted)}.n4-msg-body{font-size:12px;line-height:1.45;white-space:pre-wrap;margin-top:4px}.n4-reply-preview{font-size:9px;color:var(--muted);border-left:2px solid var(--border);padding-left:7px;margin-top:5px}.n4-reactions{display:flex;gap:4px;flex-wrap:wrap;margin-top:7px}.n4-react{border:1px solid var(--border);border-radius:999px;background:var(--panel);color:var(--text);padding:3px 7px;font-size:9px;cursor:pointer}.n4-mentions{display:flex;gap:5px;flex-wrap:wrap;margin-top:6px}.n4-mention{border:1px solid var(--border);border-radius:999px;background:var(--panel);color:var(--text);padding:4px 7px;font-size:9px;cursor:pointer}.n4-owner{font-size:9px;padding:3px 6px;border-radius:999px;background:color-mix(in srgb,#0b84ff 12%,var(--soft));color:#0b84ff}.n4-search{display:flex;gap:7px}.n4-search input{flex:1;border:1px solid var(--border);border-radius:9px;background:var(--soft);color:var(--text);padding:9px}.n4-tags{display:flex;gap:4px;flex-wrap:wrap;margin-top:6px}.n4-tag{font-size:8px;padding:3px 6px;border:1px solid var(--border);border-radius:999px;color:var(--muted)}.n4-source{font-size:8px;text-transform:uppercase;letter-spacing:.04em;color:var(--muted)}.n4-toggle{display:flex;align-items:center;justify-content:space-between;gap:12px}.n4-toggle input{width:18px;height:18px}.n4-code{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:9px;word-break:break-all}.n4-status-badge{font-size:9px;padding:3px 7px;border-radius:999px;background:var(--panel);border:1px solid var(--border)}
  @media(max-width:680px){.n4-grid,.n4-agent-picks{grid-template-columns:1fr}}
  `;document.head.appendChild(css);
  const COLORS=['#ff6b6b','#ffd166','#06d6a0','#4cc9f0','#5e60ce','#c77dff','#f72585','#fb8500'];
  const esc=v=>{const d=document.createElement('div');d.textContent=String(v??'');return d.innerHTML};
  const initials=n=>String(n||'A').split(/\s+/).slice(0,2).map(x=>x[0]).join('').toUpperCase();
  const color=a=>{let n=0;for(const ch of String(a?.id||a?.name||''))n=(n*31+ch.charCodeAt(0))>>>0;return COLORS[n%COLORS.length]};
  async function api(url,options={}){const r=await fetch(url,options),d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.detail||d.message||'Request failed');return d}
  function modal(title){const o=document.createElement('div');o.className='n4-modal';const p=document.createElement('div');p.className='n4-panel';p.innerHTML=`<div class="n4-head"><h3>${esc(title)}</h3><button type="button" class="n4-close">×</button></div>`;o.appendChild(p);document.body.appendChild(o);const close=()=>o.remove();p.querySelector('.n4-close').onclick=close;o.onclick=e=>{if(e.target===o)close()};return{overlay:o,panel:p,close}}
  function section(panel,title){const s=document.createElement('section');s.className='n4-section';s.innerHTML=`<div class="n4-section-title">${esc(title)}</div>`;panel.appendChild(s);return s}
  async function agents(){const d=await api('/platform/agents',{cache:'no-store'});window.__agentieAgents=d.items||window.__agentieAgents||[];return d.items||[]}

  async function createGroupChat(after){const list=await agents();const{panel,close}=modal('New group chat');const help=document.createElement('div');help.className='n4-help';help.textContent='Choose at least two real Agentie employees. The chat is persistent; @mentions create real Team Jobs for the mentioned agents.';panel.appendChild(help);const name=document.createElement('div');name.className='n4-field';name.innerHTML='<label>Chat name</label><input data-name placeholder="e.g. Customer Ops">';panel.appendChild(name);const picks=document.createElement('div');picks.className='n4-agent-picks';for(const a of list){const row=document.createElement('label');row.className='n4-agent-pick';row.innerHTML=`<input type="checkbox" value="${esc(a.id)}"><span class="n4-orb" style="background:${color(a)}">${esc(initials(a.name))}</span><span><strong>${esc(a.name)}</strong><small>${esc(a.job||'')}</small></span>`;picks.appendChild(row)}panel.appendChild(picks);const owner=document.createElement('div');owner.className='n4-field';owner.innerHTML='<label>Thread owner / default coordinator (optional)</label><select data-owner><option value="">No owner</option></select><div class="n4-help">Ownership is conversation context only. It does not grant delegation permission.</div>';panel.appendChild(owner);const status=document.createElement('div');panel.appendChild(status);const actions=document.createElement('div');actions.className='n4-actions';actions.innerHTML='<button type="button" class="primary">Create group chat</button>';panel.appendChild(actions);const syncOwner=()=>{const selected=[...picks.querySelectorAll('input:checked')].map(x=>x.value),select=owner.querySelector('select');const current=select.value;select.innerHTML='<option value="">No owner</option>'+list.filter(a=>selected.includes(a.id)).map(a=>`<option value="${esc(a.id)}">${esc(a.name)}</option>`).join('');if(selected.includes(current))select.value=current};picks.addEventListener('change',syncOwner);actions.firstChild.onclick=async()=>{status.className='';status.textContent='';const selected=[...picks.querySelectorAll('input:checked')].map(x=>x.value),chatName=name.querySelector('input').value.trim();if(!chatName){status.className='n4-error';status.textContent='Give the group chat a name.';return}if(selected.length<2){status.className='n4-error';status.textContent='Select at least two agents.';return}actions.firstChild.disabled=true;try{const thread=await api('/platform/agent-chats',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:chatName,participants:selected,owner_agent_id:owner.querySelector('select').value||null})});status.className='n4-success';status.textContent=`Created ${thread.name}.`;close();await after?.();openConnectedThread(thread.id)}catch(e){status.className='n4-error';status.textContent=e.message;actions.firstChild.disabled=false}}}

  async function openConnectedChats(){const{panel}=modal('Agent chats');const help=document.createElement('div');help.className='n4-help';help.textContent='Persistent conversations between your AI employees. Create a group, @mention one or several agents, watch their real work status, and see results return to the thread.';panel.appendChild(help);const actions=document.createElement('div');actions.className='n4-actions';actions.innerHTML='<button class="primary" type="button">New group chat</button>';panel.appendChild(actions);const list=document.createElement('div');list.className='n4-list';list.style.marginTop='12px';panel.appendChild(list);async function load(){const d=await api('/platform/agent-chats',{cache:'no-store'});list.replaceChildren();for(const t of d.items||[]){const row=document.createElement('div');row.className='n4-row clickable';const live=t.working_count?`${t.working_count} working`:t.queued_count?`${t.queued_count} queued`:'idle';row.innerHTML=`<div class="n4-row-head"><div><strong>${esc(t.name)}</strong><small>${esc((t.participants||[]).join(' · '))}</small></div><span class="n4-status-badge">${esc(live)}</span></div><small>${t.owner_agent_name?`Owner: ${esc(t.owner_agent_name)} · `:''}${Number(t.message_count||0)} messages</small>`;row.onclick=()=>openConnectedThread(t.id);list.appendChild(row)}if(!(d.items||[]).length)list.innerHTML='<div class="n4-help">No Agent Chats yet. Create one and choose the employees who should participate.</div>'}actions.firstChild.onclick=()=>createGroupChat(load);await load()}

  async function openConnectedThread(id){const{panel,overlay}=modal('Agent chat');const body=document.createElement('div');panel.appendChild(body);let timer=null,replyTarget=null,lastData=null;const stop=()=>{if(timer){clearInterval(timer);timer=null}};overlay.addEventListener('remove',stop);panel.querySelector('.n4-close').addEventListener('click',stop);async function load(){if(!panel.isConnected){stop();return}try{const d=await api(`/platform/agent-chats/${encodeURIComponent(id)}`,{cache:'no-store'});lastData=d;render(d)}catch(e){stop();body.innerHTML=`<div class="n4-error">${esc(e.message)}</div>`}}function render(d){panel.querySelector('h3').textContent=d.name;body.replaceChildren();const top=document.createElement('div');top.className='n4-grid';const left=document.createElement('div');left.innerHTML=`<div class="n4-help">${esc((d.participants||[]).join(' · '))}</div>`;const presence=document.createElement('div');presence.className='n4-presence';for(const p of d.presence||[]){const chip=document.createElement('div');chip.className='n4-presence-chip';chip.innerHTML=`<span class="n4-dot ${esc(p.status)}"></span><b>${esc(p.agent_name)}</b><span>${esc(p.status)}</span>${p.outstanding_tasks?`<span>· ${p.outstanding_tasks}</span>`:''}`;presence.appendChild(chip)}left.appendChild(presence);const right=document.createElement('div');right.className='n4-field';right.style.marginTop='0';right.innerHTML=`<label>Thread owner</label><select data-owner><option value="">No owner</option>${(d.participant_ids||[]).map((aid,i)=>`<option value="${esc(aid)}" ${aid===d.owner_agent_id?'selected':''}>${esc((d.participants||[])[i]||aid)}</option>`).join('')}</select><div class="n4-help">Owner is coordination context, not permission.</div>`;right.querySelector('select').onchange=async e=>{await api(`/platform/agent-chats/${encodeURIComponent(id)}/owner`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({agent_id:e.target.value||null})});await load()};top.append(left,right);body.appendChild(top);const messages=document.createElement('div');messages.style.marginTop='12px';const map=Object.fromEntries((d.messages||[]).map(x=>[x.id,x]));for(const m of d.messages||[]){const row=document.createElement('div');row.className='n4-msg'+((m.metadata||{}).reply_to_message_id?' reply':'');const parent=map[(m.metadata||{}).reply_to_message_id],job=m.job||null;row.innerHTML=`<div class="n4-msg-meta"><b>${esc(m.sender_name||m.sender_type)}</b><span>${esc(m.at||'')}</span>${d.owner_agent_id&&m.sender_id===d.owner_agent_id?'<span class="n4-owner">owner</span>':''}${job?`<span>job ${esc(job.status||'')}</span>${job.replan_count?`<span>replanned ${Number(job.replan_count)}</span>`:''}`:''}</div>`;if(parent){const prev=document.createElement('div');prev.className='n4-reply-preview';prev.textContent=`↪ ${parent.sender_name||parent.sender_type}: ${String(parent.message||'').slice(0,120)}`;row.appendChild(prev)}const text=document.createElement('div');text.className='n4-msg-body';text.textContent=m.message||'';row.appendChild(text);if(job?.final_output&&m.sender_type!=='agent'){const result=document.createElement('div');result.className='n4-reply-preview';result.textContent=job.final_output;row.appendChild(result)}const reacts=document.createElement('div');reacts.className='n4-reactions';for(const r of m.reactions||[]){const chip=document.createElement('span');chip.className='n4-react';chip.textContent=`${r.reaction} ${r.actor_name||''}`;reacts.appendChild(chip)}for(const emoji of ['👍','✅','❗','❤️']){const b=document.createElement('button');b.className='n4-react';b.type='button';b.textContent=emoji;b.onclick=async()=>{await api(`/platform/agent-chats/${encodeURIComponent(id)}/messages/${encodeURIComponent(m.id)}/reaction`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({reaction:emoji})});await load()};reacts.appendChild(b)}const reply=document.createElement('button');reply.className='n4-react';reply.type='button';reply.textContent='Reply';reply.onclick=()=>{replyTarget=m;render(d)};reacts.appendChild(reply);row.appendChild(reacts);messages.appendChild(row)}body.appendChild(messages);const composer=document.createElement('div');composer.className='n4-section';if(replyTarget){const preview=document.createElement('div');preview.className='n4-reply-preview';preview.textContent=`Replying to ${replyTarget.sender_name||'message'}: ${String(replyTarget.message||'').slice(0,120)}`;composer.appendChild(preview)}const mentions=document.createElement('div');mentions.className='n4-mentions';for(let i=0;i<(d.participants||[]).length;i++){const b=document.createElement('button');b.type='button';b.className='n4-mention';b.textContent=`@${d.participants[i]}`;b.onclick=()=>{const ta=composer.querySelector('textarea');const token=`@${d.participants[i]} `;if(!ta.value.includes(token.trim()))ta.value=(ta.value+' '+token).trimStart();ta.focus()};mentions.appendChild(b)}composer.appendChild(mentions);const field=document.createElement('div');field.className='n4-field';field.innerHTML='<label>Message or task</label><textarea placeholder="@Ben @Mira compare the options and report back"></textarea>';composer.appendChild(field);const acts=document.createElement('div');acts.className='n4-actions';acts.innerHTML=`<button type="button" class="primary">${replyTarget?'Reply':'Send'}</button>${replyTarget?'<button type="button" data-cancel>Cancel reply</button>':''}`;composer.appendChild(acts);acts.querySelector('[data-cancel]')?.addEventListener('click',()=>{replyTarget=null;render(d)});acts.querySelector('.primary').onclick=async()=>{const text=field.querySelector('textarea').value.trim();if(!text)return;acts.querySelector('.primary').disabled=true;try{if(replyTarget){await api(`/platform/agent-chats/${encodeURIComponent(id)}/messages/${encodeURIComponent(replyTarget.id)}/reply`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:text})});replyTarget=null}else await api(`/platform/agent-chats/${encodeURIComponent(id)}/messages`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:text})});await load()}catch(e){const err=document.createElement('div');err.className='n4-error';err.textContent=e.message;composer.appendChild(err)}};body.appendChild(composer)}await load();timer=setInterval(load,2200)}

  async function openMarketplace(){const listAgents=await agents();const active=document.querySelector('#persistentAgentList .agent-row.active .agent-copy strong')?.textContent?.trim();const activeAgent=listAgents.find(a=>a.name===active);const{panel}=modal('Skill Marketplace');const help=document.createElement('div');help.className='n4-help';help.textContent='Agentie-curated/local reusable workflows plus your installed Skills. Nothing is silently installed, assigned or presented as a remote community marketplace.';panel.appendChild(help);const search=document.createElement('div');search.className='n4-search';search.innerHTML=`<input placeholder="Search Skills, e.g. invoice, research, calendar"><select><option value="">For any agent</option>${listAgents.map(a=>`<option value="${esc(a.id)}" ${activeAgent?.id===a.id?'selected':''}>Recommend for ${esc(a.name)}</option>`).join('')}</select><button type="button">Search</button>`;search.querySelector('button').className='n4-react';panel.appendChild(search);const list=document.createElement('div');list.className='n4-list';list.style.marginTop='12px';panel.appendChild(list);async function load(){const q=search.querySelector('input').value.trim(),aid=search.querySelector('select').value;const d=await api(`/platform/skills/marketplace?q=${encodeURIComponent(q)}${aid?`&agent_id=${encodeURIComponent(aid)}`:''}`,{cache:'no-store'});list.replaceChildren();for(const item of d.items||[]){const row=document.createElement('div');row.className='n4-row';const source=item.source==='installed'?'Installed Skill':'Agentie curated';row.innerHTML=`<div class="n4-row-head"><div><span class="n4-source">${esc(source)}</span><strong>${esc(item.name)}</strong><small>${esc(item.description||'')}</small></div>${item.recommended?'<span class="n4-status-badge">Recommended</span>':''}</div><div class="n4-tags">${(item.tags||[]).slice(0,7).map(t=>`<span class="n4-tag">${esc(t)}</span>`).join('')}</div><small>${(item.required_access||[]).length?`Requires: ${esc(item.required_access.join(', '))}`:'No special access declared'}</small>`;const a=document.createElement('div');a.className='n4-actions';if(item.source!=='installed'&&!item.installed){const install=document.createElement('button');install.className='primary';install.textContent='Install draft';install.onclick=async()=>{install.disabled=true;await api(`/platform/skills/marketplace/${encodeURIComponent(item.id)}/install`,{method:'POST'});await load()};a.appendChild(install)}if(aid){const assign=document.createElement('button');assign.textContent=item.installed||item.source==='installed'?'Assign':'Install + assign';assign.onclick=async()=>{assign.disabled=true;await api(`/platform/skills/marketplace/${encodeURIComponent(item.id)}/assign/${encodeURIComponent(aid)}`,{method:'POST'});await load()};a.appendChild(assign)}if(item.source==='installed'&&item.skill_id){const share=document.createElement('button');share.textContent='Share / export';share.onclick=async()=>{const d=await api(`/platform/skills/${encodeURIComponent(item.skill_id)}/share`,{method:'POST'});if(typeof window.addAssistant==='function')window.addAssistant(`Exported ${item.name}.`,d.card||null)};a.appendChild(share)}row.appendChild(a);list.appendChild(row)}if(!(d.items||[]).length)list.innerHTML='<div class="n4-help">No matching Skills found.</div>'}search.querySelector('button').onclick=load;search.querySelector('input').addEventListener('keydown',e=>{if(e.key==='Enter')load()});search.querySelector('select').onchange=load;await load()}

  async function openAutomationNext(){const{panel}=modal('Automation');const help=document.createElement('div');help.className='n4-help';help.textContent='Native Agentie events and real Google Workspace event sources feed the existing Routine engine. Google sources only poll after you enable them and only when the connected Google Workspace MCP can actually be inspected.';panel.appendChild(help);const google=section(panel,'Google Workspace event sources');const status=document.createElement('div');google.appendChild(status);async function loadGoogle(verify=false){const d=await api(`/platform/google-events/status?verify=${verify?'true':'false'}`,{cache:'no-store'});status.replaceChildren();const head=document.createElement('div');head.className='n4-row';head.innerHTML=`<div class="n4-row-head"><div><strong>Google Workspace</strong><small>${d.registered?'MCP registered':'MCP not registered'} · ${d.configured?'credentials configured':'credentials not configured'}${d.connected===true?' · connection verified':d.connected===false?' · connection failed':''}</small></div><span class="n4-status-badge">${d.last_poll_at?`last poll ${esc(d.last_poll_at)}`:'not polled yet'}</span></div>${d.connection_error?`<div class="n4-error">${esc(d.connection_error)}</div>`:''}<div class="n4-actions"><button type="button" data-verify>Verify connection</button><button type="button" data-poll>Poll now</button></div>`;status.appendChild(head);head.querySelector('[data-verify]').onclick=()=>loadGoogle(true);head.querySelector('[data-poll]').onclick=async e=>{e.target.disabled=true;const r=await api('/platform/google-events/poll-now',{method:'POST'});e.target.disabled=false;alert(r.polled?`Poll finished · ${r.events||0} new event(s)`:r.reason||'Nothing polled');await loadGoogle(false)};const toggles=document.createElement('div');toggles.className='n4-list';toggles.style.marginTop='8px';for(const src of d.sources||[]){if(src.id==='drive')continue;const row=document.createElement('label');row.className='n4-row n4-toggle';row.innerHTML=`<span><strong>${esc(src.label)}</strong><small>${esc(src.event_type)}</small></span><input type="checkbox" ${src.enabled?'checked':''}>`;row.querySelector('input').onchange=async e=>{const body=src.id==='gmail'?{gmail_enabled:e.target.checked}:{calendar_enabled:e.target.checked};try{await api('/platform/google-events/settings',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});await loadGoogle(false)}catch(err){e.target.checked=!e.target.checked;alert(err.message)}};toggles.appendChild(row)}status.appendChild(toggles);const drive=document.createElement('div');drive.className='n4-row';drive.style.marginTop='8px';drive.innerHTML='<strong>Google Drive watches</strong><small>Watch explicit files/folders for changes. This does not pretend the current MCP has a global Drive push feed.</small>';for(const w of d.drive_watches||[]){const item=document.createElement('div');item.className='n4-actions';item.innerHTML=`<span class="n4-code">${esc(w.label||w.item_id)} · ${esc(w.kind)}</span><button type="button">Remove</button>`;item.querySelector('button').onclick=async()=>{await api(`/platform/google-events/drive-watch/${encodeURIComponent(w.id)}`,{method:'DELETE'});await loadGoogle(false)};drive.appendChild(item)}const form=document.createElement('div');form.className='n4-grid';form.innerHTML='<div class="n4-field"><label>Drive file/folder ID</label><input data-id></div><div class="n4-field"><label>Kind</label><select data-kind><option value="file">File</option><option value="folder">Folder</option></select></div><div class="n4-field"><label>Label (optional)</label><input data-label></div>';drive.appendChild(form);const acts=document.createElement('div');acts.className='n4-actions';acts.innerHTML='<button type="button">Add Drive watch</button>';drive.appendChild(acts);acts.firstChild.onclick=async()=>{const id=form.querySelector('[data-id]').value.trim();if(!id)return;await api('/platform/google-events/drive-watch',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({item_id:id,kind:form.querySelector('[data-kind]').value,label:form.querySelector('[data-label]').value.trim()})});await loadGoogle(false)};status.appendChild(drive)}await loadGoogle(false);const native=section(panel,'Routine event examples');native.innerHTML+='<div class="n4-row"><strong>Use these in event Routines</strong><small>email.received · calendar.event.started · drive.file.changed · drive.folder.changed · file.uploaded · whatsapp.message.received · agent_thread.message · agent_thread.agent_reply · team_job.completed · skill_run.completed</small></div>'}

  function installLaunchers(){const sidebar=document.querySelector('.sidebar'),search=sidebar?.querySelector('.agent-search');if(!sidebar||!search)return;let anchor=search;const defs=[['n4-chats','💬 Agent chats',openConnectedChats],['n4-market','⚡ Skill Marketplace',openMarketplace],['n4-auto','↻ Automation',openAutomationNext]];for(const [cls,label,fn] of defs){let b=sidebar.querySelector('.'+cls);if(!b){b=document.createElement('button');b.type='button';b.className=`n4-launch ${cls}`;b.textContent=label;b.onclick=fn;anchor.after(b)}anchor=b}}
  let queued=false;new MutationObserver(()=>{if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;installLaunchers()})}).observe(document.body,{childList:true,subtree:true});setTimeout(installLaunchers,100);
})();
