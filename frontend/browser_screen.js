(()=>{
  const messages=document.getElementById('messages'),input=document.getElementById('messageInput'),send=document.getElementById('sendButton'),agentType=document.getElementById('agentType');
  if(!messages||!input||!send||window.__agentieInlineComputer)return;window.__agentieInlineComputer=true;
  const style=document.createElement('style');style.textContent=`
    .inline-computer-row{display:flex;margin:10px 0 14px}.inline-computer{width:min(680px,92vw);border:1px solid var(--border);border-radius:15px;background:var(--panel);overflow:hidden}
    .inline-computer-head{height:42px;display:flex;align-items:center;gap:8px;padding:0 11px 0 13px;border-bottom:1px solid var(--border)}.inline-computer-dot{width:8px;height:8px;border-radius:50%;background:#8a8a8a}.inline-computer-dot.active{background:#36a269;box-shadow:0 0 0 3px rgba(54,162,105,.14)}.inline-computer-dot.error{background:#d94a4a}.inline-computer-title{font-size:12px;font-weight:700;flex:1}.inline-computer-status{font-size:10px;color:var(--muted);max-width:190px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
    .inline-computer-head button,.computer-toolbar button{border:1px solid var(--border);border-radius:7px;padding:4px 7px;background:transparent;color:var(--muted);cursor:pointer;font-size:11px}.inline-computer-stop:disabled{opacity:.35;cursor:default}
    .inline-computer-viewport{position:relative;background:#111;aspect-ratio:16/9;overflow:hidden;cursor:crosshair}.inline-computer-viewport img{width:100%;height:100%;display:block;object-fit:contain;background:#111;user-select:none;-webkit-user-drag:none}.inline-computer-placeholder{position:absolute;inset:0;display:grid;place-items:center;color:#9b9b9b;font-size:12px;text-align:center;padding:20px;pointer-events:none}.computer-click-dot{position:absolute;width:14px;height:14px;border:2px solid #fff;border-radius:50%;transform:translate(-50%,-50%);box-shadow:0 0 0 2px rgba(0,0,0,.45);pointer-events:none;animation:clickfade .65s forwards}@keyframes clickfade{to{opacity:0;transform:translate(-50%,-50%) scale(1.8)}}
    .inline-computer-foot{padding:8px 11px 10px}.inline-computer-url{font-size:10px;color:var(--muted);overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.inline-computer-detail{font-size:11px;margin-top:3px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.computer-toolbar{display:flex;gap:5px;margin-top:8px;align-items:center}.computer-toolbar .computer-type{flex:1;min-width:0;border:1px solid var(--border);border-radius:8px;background:var(--panel);color:var(--text);padding:6px 8px;font-size:11px;outline:none}.computer-toolbar button:hover{background:var(--soft)}.computer-help{font-size:10px;color:var(--muted);margin-top:6px}
    .web-snapshot-card,.browser-actions-card,.browser-approval-card{width:min(680px,92vw)}.web-snapshot-card img{display:block;width:100%;max-height:520px;object-fit:contain;border-radius:11px;border:1px solid var(--border);background:#111;margin-top:10px}.web-snapshot-url{font-size:10px;color:var(--muted);margin-top:7px;overflow-wrap:anywhere}.web-snapshot-excerpt{font-size:12px;line-height:1.45;margin-top:8px;color:var(--muted)}.browser-actions-list{display:grid;gap:6px;margin-top:10px}.browser-action{padding:8px 10px;border-radius:9px;background:var(--soft);font-size:12px}.browser-approval-step{margin-top:10px;padding:10px;border-radius:10px;background:var(--soft);font-size:12px}.browser-approval-actions{display:flex;gap:7px;margin-top:10px}.browser-approval-actions button{border:1px solid var(--border);border-radius:8px;padding:6px 10px;background:var(--panel);color:var(--text);cursor:pointer}.browser-approval-actions .approve{background:var(--accent);color:var(--accent-text);border-color:var(--accent)}
    @media(max-width:760px){.inline-computer,.web-snapshot-card,.browser-actions-card,.browser-approval-card{width:min(100%,92vw)}}`;
  document.head.appendChild(style);

  const browserIntent=text=>/https?:\/\//i.test(text)&&/\b(open|visit|check|inspect|look at|view|screenshot|snapshot|capture|monitor|watch|click|type|fill|search|scroll|press|browse|navigate)\b/i.test(text);
  const continuationIntent=text=>/^\s*(click|type|fill|scroll|press|go back|back|forward|new tab|close tab|screenshot|take a screenshot|search for)\b/i.test(text);
  let currentComputer=null;

  function computerCard(){const row=document.createElement('div');row.className='assistant-row inline-computer-row';row.innerHTML=`<div class="inline-computer"><div class="inline-computer-head"><span class="inline-computer-dot"></span><span class="inline-computer-title">Computer</span><span class="inline-computer-status">Starting</span><button class="inline-computer-stop" type="button">Stop</button></div><div class="inline-computer-viewport"><img alt="Agentie browser view" style="display:none"><div class="inline-computer-placeholder">Starting Agentie's browser…</div></div><div class="inline-computer-foot"><div class="inline-computer-url"></div><div class="inline-computer-detail">Preparing browser task</div><div class="computer-toolbar"><button data-control="back" title="Back">←</button><button data-control="forward" title="Forward">→</button><button data-control="reload" title="Reload">↻</button><input class="computer-type" type="text" placeholder="Type into the focused field…"><button data-key="Enter">Enter</button><button data-key="Tab">Tab</button><button data-key="Escape">Esc</button></div><div class="computer-help">Click directly on the screen to interact. Scroll over the screen with your mouse wheel.</div></div></div>`;return row}
  async function initialState(){try{const r=await fetch('/browser/live/state',{cache:'no-store'});return r.ok?await r.json():{}}catch(_){return {}}}
  async function internalControl(command,computer){
    try{
      const r=await fetch('/agent/run',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:`Browser control: ${command}`,agent_type:agentType?.value||'general'})});const d=await r.json();if(!r.ok)throw new Error(d.detail||'Browser control failed');
      if(d.card?.type==='browser_approval'){window.addAssistant?.(d.message||'',d.card)}
      else if(d.card?.type==='browser_actions'&&!d.card.internal_control){window.addAssistant?.(d.message||'',d.card)}
      return d;
    }catch(err){if(computer){const detail=computer.querySelector('.inline-computer-detail');if(detail)detail.textContent=err.message||'Browser control failed'}}
  }
  function showClick(viewport,x,y){const dot=document.createElement('span');dot.className='computer-click-dot';dot.style.left=`${x}px`;dot.style.top=`${y}px`;viewport.appendChild(dot);setTimeout(()=>dot.remove(),700)}

  async function attachComputer(text){
    if(!(browserIntent(text)||continuationIntent(text)))return;
    const row=computerCard();const working=[...messages.querySelectorAll('.assistant-row')].reverse().find(x=>x.querySelector('.working'));if(working)messages.insertBefore(row,working);else messages.appendChild(row);currentComputer=row;window.scrollTo({top:document.body.scrollHeight,behavior:'smooth'});
    const baseline=await initialState();const root=row.querySelector('.inline-computer'),dot=root.querySelector('.inline-computer-dot'),status=root.querySelector('.inline-computer-status'),img=root.querySelector('img'),placeholder=root.querySelector('.inline-computer-placeholder'),viewport=root.querySelector('.inline-computer-viewport'),url=root.querySelector('.inline-computer-url'),detail=root.querySelector('.inline-computer-detail'),stop=root.querySelector('.inline-computer-stop'),typeBox=root.querySelector('.computer-type');let lastVersion=Number(baseline.frame_version||0),seenActivity=false,ticks=0,closed=false;
    stop.onclick=async()=>{stop.disabled=true;try{await fetch('/browser/live/stop',{method:'POST'})}catch(_){stop.disabled=false}};
    root.querySelectorAll('[data-control]').forEach(b=>b.onclick=()=>internalControl(b.dataset.control,root));root.querySelectorAll('[data-key]').forEach(b=>b.onclick=()=>internalControl(`key ${b.dataset.key}`,root));
    typeBox.addEventListener('keydown',e=>{if(e.key==='Enter'&&typeBox.value){e.preventDefault();const value=typeBox.value;typeBox.value='';internalControl(`type focused: ${value}`,root)}});
    viewport.addEventListener('click',e=>{if(img.style.display==='none'||!img.naturalWidth||!img.naturalHeight)return;const rect=img.getBoundingClientRect(),rawX=e.clientX-rect.left,rawY=e.clientY-rect.top,scale=Math.min(rect.width/img.naturalWidth,rect.height/img.naturalHeight),drawW=img.naturalWidth*scale,drawH=img.naturalHeight*scale,offX=(rect.width-drawW)/2,offY=(rect.height-drawH)/2,x=rawX-offX,y=rawY-offY;if(x<0||y<0||x>drawW||y>drawH)return;showClick(viewport,e.clientX-viewport.getBoundingClientRect().left,e.clientY-viewport.getBoundingClientRect().top);const bx=x/drawW*1440,by=y/drawH*900;internalControl(`click at ${bx.toFixed(1)},${by.toFixed(1)}`,root)});
    viewport.addEventListener('wheel',e=>{if(img.style.display==='none')return;e.preventDefault();internalControl(`scroll ${Math.max(-1400,Math.min(1400,e.deltaY*2)).toFixed(0)}`,root)},{passive:false});
    const timer=setInterval(async()=>{if(closed)return;ticks++;try{const r=await fetch('/browser/live/state',{cache:'no-store'});if(!r.ok)return;const s=await r.json(),version=Number(s.frame_version||0),active=!!s.active;const changed=active||version!==Number(baseline.frame_version||0)||String(s.updated_at||'')!==String(baseline.updated_at||'');if(changed)seenActivity=true;if(!seenActivity&&ticks<40)return;dot.classList.toggle('active',active);dot.classList.toggle('error',s.status==='error');status.textContent=active?(s.status||'Working'):(s.status==='done'?'Ready':s.status==='error'?'Error':s.status==='paused'?'Paused':s.status||'Idle');url.textContent=s.url||'';detail.textContent=s.error||s.detail||'';stop.disabled=!active;if(version!==lastVersion&&version>0){lastVersion=version;img.src=`/browser/live/frame?v=${version}&t=${Date.now()}`;img.style.display='block';placeholder.style.display='none'}if(seenActivity&&!active&&['error'].includes(s.status)){closed=true;clearInterval(timer);stop.disabled=true}}catch(_){ }},450);
    setTimeout(()=>{if(!closed){closed=true;clearInterval(timer);stop.disabled=true}},10*60*1000);
  }

  function scheduleComputer(text){if(!(browserIntent(text)||continuationIntent(text)))return;setTimeout(()=>attachComputer(text),0)}
  send.addEventListener('click',()=>{const text=input.value.trim();if(text)scheduleComputer(text)},true);input.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){const text=input.value.trim();if(text)scheduleComputer(text)}},true);

  function resend(text){if(!text)return;input.value=text;input.dispatchEvent(new Event('input',{bubbles:true}));send.click()}
  const previousRender=window.renderCard;if(typeof previousRender==='function')window.renderCard=function(card,message){
    if(!card||!['web_snapshot','browser_actions','browser_approval'].includes(card.type))return previousRender(card,message);
    if(card.type==='browser_approval'){const wrap=document.createElement('div');wrap.className='card-wrap browser-approval-card';const el=document.createElement('div');el.className='result-card';wrap.appendChild(el);if(message){const top=document.createElement('div');top.className='card-topline';top.textContent=message;el.appendChild(top)}const title=document.createElement('div');title.className='card-title';title.textContent='🛡 Browser approval';el.appendChild(title);const u=document.createElement('div');u.className='web-snapshot-url';u.textContent=card.url||'';el.appendChild(u);const step=document.createElement('div');step.className='browser-approval-step';step.textContent=card.step||'Consequential browser action';el.appendChild(step);const actions=document.createElement('div');actions.className='browser-approval-actions';const approve=document.createElement('button'),deny=document.createElement('button');approve.className='approve';approve.textContent='Approve once';deny.textContent='Deny';actions.append(approve,deny);el.appendChild(actions);async function resolve(allowed){approve.disabled=true;deny.disabled=true;try{const r=await fetch(`/approvals/${card.approval.id}/resolve`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({approved:allowed})});if(!r.ok)throw new Error('Could not resolve approval');title.textContent=allowed?'✓ Browser action approved':'Browser action denied';actions.remove();if(allowed)setTimeout(()=>resend(card.command),80)}catch(err){approve.disabled=false;deny.disabled=false;const note=document.createElement('div');note.className='card-meta';note.textContent=err.message;el.appendChild(note)}}approve.onclick=()=>resolve(true);deny.onclick=()=>resolve(false);return wrap}
    if(card.type==='browser_actions'){const wrap=document.createElement('div');wrap.className='card-wrap browser-actions-card';const el=document.createElement('div');el.className='result-card';wrap.appendChild(el);if(message){const top=document.createElement('div');top.className='card-topline';top.textContent=message;el.appendChild(top)}const title=document.createElement('div');title.className='card-title';title.textContent=`🖥 ${card.title||'Browser actions'}`;el.appendChild(title);const u=document.createElement('div');u.className='web-snapshot-url';u.textContent=card.url||'';el.appendChild(u);const list=document.createElement('div');list.className='browser-actions-list';(card.actions||[]).forEach(action=>{const row=document.createElement('div');row.className='browser-action';row.textContent=action;list.appendChild(row)});el.appendChild(list);return wrap}
    const wrap=document.createElement('div');wrap.className='card-wrap web-snapshot-card';const el=document.createElement('div');el.className='result-card';wrap.appendChild(el);if(message){const top=document.createElement('div');top.className='card-topline';top.textContent=message;el.appendChild(top)}const title=document.createElement('div');title.className='card-title';title.textContent=`🌐 ${card.title||'Website snapshot'}`;el.appendChild(title);const shot=document.createElement('img');shot.src=card.image_url;shot.alt=`Screenshot of ${card.title||card.url||'website'}`;el.appendChild(shot);const u=document.createElement('div');u.className='web-snapshot-url';u.textContent=card.url||'';el.appendChild(u);const meta=document.createElement('div');meta.className='card-meta';const captured=card.captured_at?new Date(card.captured_at).toLocaleString():'';meta.textContent=`${card.changed?'Changed':'Snapshot'}${captured?' · '+captured:''}`;el.appendChild(meta);if(card.excerpt){const ex=document.createElement('div');ex.className='web-snapshot-excerpt';ex.textContent=card.excerpt;el.appendChild(ex)}return wrap};
})();