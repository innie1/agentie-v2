(()=>{
  if(window.__agentiePlatformUI)return;window.__agentiePlatformUI=true;
  const style=document.createElement('style');style.textContent=`
  .base-agent-label,#agentType{display:none!important}.platform-modal{position:fixed;inset:0;z-index:11000;background:rgba(0,0,0,.46);display:grid;place-items:center;padding:18px}.platform-panel{width:min(720px,100%);max-height:90vh;overflow:auto;background:var(--panel);border:1px solid var(--border);border-radius:18px;padding:18px;box-shadow:0 24px 70px rgba(0,0,0,.28)}.platform-head{display:flex;align-items:center;justify-content:space-between;gap:12px}.platform-head h3{margin:0;font-size:17px}.platform-close{border:0;background:transparent;color:var(--muted);font-size:22px;cursor:pointer}.platform-help{font-size:11px;color:var(--muted);line-height:1.45;margin-top:5px}.platform-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:14px}.platform-field{display:grid;gap:5px}.platform-field.full{grid-column:1/-1}.platform-field label{font-size:11px;font-weight:650}.platform-field input,.platform-field textarea,.platform-field select{width:100%;box-sizing:border-box;border:1px solid var(--border);border-radius:9px;background:var(--soft);color:var(--text);padding:9px 10px;outline:0}.platform-field textarea{min-height:78px;resize:vertical}.platform-section{margin-top:16px;padding-top:13px;border-top:1px solid var(--border)}.platform-section-title{font-size:10px;text-transform:uppercase;letter-spacing:.06em;color:var(--muted);margin-bottom:8px}.platform-options{display:grid;gap:7px}.platform-option{display:grid;grid-template-columns:20px 1fr;gap:8px;padding:8px;border:1px solid var(--border);border-radius:9px;background:var(--soft)}.platform-option strong,.platform-option small{display:block}.platform-option strong{font-size:11px}.platform-option small{font-size:9px;color:var(--muted);margin-top:2px}.platform-actions{display:flex;justify-content:flex-end;gap:8px;margin-top:16px}.platform-actions button{border:1px solid var(--border);border-radius:9px;background:var(--panel);color:var(--text);padding:8px 12px;cursor:pointer}.platform-actions .primary{background:#0b84ff;color:#fff;border-color:#0b84ff}.platform-status{font-size:11px;color:var(--muted);margin-top:10px}.platform-chats-launch,.platform-activity-launch,.platform-skills-launch{width:100%;margin-top:8px;border:1px solid var(--border);background:transparent;color:var(--text);border-radius:10px;padding:8px 10px;text-align:left;cursor:pointer}.platform-chats-launch:hover,.platform-activity-launch:hover,.platform-skills-launch:hover{background:var(--soft)}.platform-thread-list{display:grid;gap:8px;margin-top:12px}.platform-thread-row{padding:10px;border:1px solid var(--border);border-radius:10px;background:var(--soft);cursor:pointer}.platform-thread-row strong,.platform-thread-row small{display:block}.platform-thread-row small{color:var(--muted);font-size:10px;margin-top:3px}.platform-thread-message{padding:8px 10px;border-radius:10px;background:var(--soft);margin-top:7px;font-size:12px;line-height:1.45}.platform-thread-message small{display:block;color:var(--muted);font-size:9px;margin-bottom:3px}.platform-skill-new{border:1px solid var(--border);border-radius:8px;background:var(--panel);color:var(--text);padding:5px 8px;font-size:10px;cursor:pointer;margin-left:auto;margin-right:8px}.platform-rec{padding:8px 10px;border:1px solid var(--border);border-radius:9px;background:var(--soft);margin-top:6px}.platform-rec strong{font-size:11px;display:block}.platform-rec small{font-size:9px;color:var(--muted)}@media(max-width:640px){.platform-grid{grid-template-columns:1fr}.platform-field.full{grid-column:1}}
  `;document.head.appendChild(style);
  const esc=v=>{const d=document.createElement('div');d.textContent=String(v??'');return d.innerHTML};
  function activeAgent(){const row=document.querySelector('#persistentAgentList .agent-row.active');if(!row)return null;const strong=row.querySelector('.agent-copy strong'),name=[...(strong?.childNodes||[])].find(n=>n.nodeType===Node.TEXT_NODE)?.textContent?.trim()||strong?.textContent?.trim();return (window.__agentieAgents||[]).find(a=>a.name===name)||null}
  function modal(title){const overlay=document.createElement('div');overlay.className='platform-modal';const panel=document.createElement('div');panel.className='platform-panel';panel.innerHTML=`<div class="platform-head"><h3>${esc(title)}</h3><button class="platform-close" type="button">×</button></div>`;overlay.appendChild(panel);document.body.appendChild(overlay);const close=()=>overlay.remove();panel.querySelector('.platform-close').onclick=close;overlay.onclick=e=>{if(e.target===overlay)close()};return{overlay,panel,close}}
  async function jsonFetch(url,options={}){const r=await fetch(url,options),d=await r.json();if(!r.ok)throw new Error(d.detail||d.message||'Request failed');return d}
  async function agentRun(message,agent=activeAgent()){return jsonFetch('/agent/run',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message,agent_type:'general',session_id:agent?`agent:${agent.id}:main`:'ui:base'})})}
  function showRun(d){if(typeof window.addAssistant==='function')window.addAssistant(d.message||d.result||'',d.card||null)}
  async function openBuilder(){
    const {panel,close}=modal('Create an agent');const intro=document.createElement('div');intro.className='platform-help';intro.textContent='Describe the job you want this agent to own. Agentie will propose a configuration; you control the final skills, plugins, permissions and instructions.';panel.appendChild(intro);
    const first=document.createElement('div');first.className='platform-grid';first.innerHTML=`<div class="platform-field"><label>Name (optional)</label><input data-name placeholder="e.g. Ben"></div><div class="platform-field"><label>Short job label (optional)</label><input data-job placeholder="e.g. Customer support"></div><div class="platform-field full"><label>What should this agent own?</label><textarea data-description placeholder="Handle customer support on Gmail and WhatsApp, escalate refunds, keep a history and send me a daily summary."></textarea></div>`;panel.appendChild(first);const actions=document.createElement('div');actions.className='platform-actions';actions.innerHTML='<button type="button" class="primary" data-draft>Build configuration</button>';panel.appendChild(actions);const status=document.createElement('div');status.className='platform-status';panel.appendChild(status);
    first.querySelector('[data-description]').focus();actions.querySelector('[data-draft]').onclick=async()=>{const description=first.querySelector('[data-description]').value.trim();if(!description){status.textContent='Describe the job first.';return}const b=actions.querySelector('[data-draft]');b.disabled=true;status.textContent='Building from the capabilities already in Agentie…';try{const draft=await jsonFetch('/agent-builder/draft',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({description,name:first.querySelector('[data-name]').value.trim(),job:first.querySelector('[data-job]').value.trim()})});renderDraft(panel,draft,close)}catch(e){status.textContent=e.message}finally{b.disabled=false}}
  }
  function option(item,kind){const row=document.createElement('label');row.className='platform-option';const check=document.createElement('input');check.type='checkbox';check.checked=kind==='skill' ? Number(item.score||0)>0 : Number(item.score||0)>0&&!!item.installed;check.dataset.optionId=item.id;check.dataset.optionKind=kind;const copy=document.createElement('div');const strong=document.createElement('strong');strong.textContent=item.name||item.id;const small=document.createElement('small');small.textContent=(item.description||'')+(kind==='plugin'&&!item.installed?' · Connect from Plugins first':'');copy.append(strong,small);row.append(check,copy);return row}
  function renderDraft(panel,draft,close){
    [...panel.children].slice(1).forEach(x=>x.remove());const help=document.createElement('div');help.className='platform-help';help.textContent='This is a proposal, not a fixed agent type. Edit anything before creating it.';panel.appendChild(help);const grid=document.createElement('div');grid.className='platform-grid';grid.innerHTML=`<div class="platform-field"><label>Name</label><input data-name value="${esc(draft.name||'')}"></div><div class="platform-field"><label>Job / ownership</label><input data-job value="${esc(draft.job||'')}"></div><div class="platform-field full"><label>Goal</label><textarea data-goal>${esc(draft.goal||'')}</textarea></div><div class="platform-field full"><label>Working style</label><textarea data-style>${esc(draft.working_style||'')}</textarea></div><div class="platform-field full"><label>Responsibilities · one per line</label><textarea data-responsibilities>${esc((draft.responsibilities||[]).join('\n'))}</textarea></div><div class="platform-field full"><label>Instructions</label><textarea data-instructions>${esc(draft.instructions||'')}</textarea></div><div class="platform-field"><label>Reports to</label><select data-manager><option value="">No manager</option>${(window.__agentieAgents||[]).map(a=>`<option value="${esc(a.id)}">${esc(a.name)} · ${esc(a.role||'')}</option>`).join('')}</select></div><div class="platform-field"><label><input type="checkbox" data-delegate> Can delegate work to other agents</label><div class="platform-help">This is a permission, not something inferred from the job title.</div></div>`;panel.appendChild(grid);if(draft.recommended_manager?.id)grid.querySelector('[data-manager]').value=draft.recommended_manager.id;grid.querySelector('[data-delegate]').checked=!!draft.can_delegate_recommended;
    const skills=document.createElement('section');skills.className='platform-section';skills.innerHTML='<div class="platform-section-title">Recommended capabilities & reusable skills</div><div class="platform-options"></div>';panel.appendChild(skills);(draft.skills||[]).forEach(x=>skills.querySelector('.platform-options').appendChild(option(x,'skill')));if(!(draft.skills||[]).length)skills.querySelector('.platform-options').innerHTML='<div class="platform-help">No special capability matched. You can assign more later from the agent profile.</div>';
    const plugins=document.createElement('section');plugins.className='platform-section';plugins.innerHTML='<div class="platform-section-title">Recommended plugins / MCPs</div><div class="platform-options"></div>';panel.appendChild(plugins);(draft.plugins||[]).forEach(x=>plugins.querySelector('.platform-options').appendChild(option(x,'plugin')));if(!(draft.plugins||[]).length)plugins.querySelector('.platform-options').innerHTML='<div class="platform-help">No plugin is required by this description.</div>';
    if(draft.recommended_manager||(draft.recommended_collaborators||[]).length){const team=document.createElement('section');team.className='platform-section';team.innerHTML='<div class="platform-section-title">Existing team fit</div>';if(draft.recommended_manager)team.innerHTML+=`<div class="platform-rec"><strong>Suggested coordinator · ${esc(draft.recommended_manager.name)}</strong><small>${esc(draft.recommended_manager.reason||'')}</small></div>`;(draft.recommended_collaborators||[]).forEach(x=>team.innerHTML+=`<div class="platform-rec"><strong>${esc(x.name)} · ${esc(x.job||'')}</strong><small>Existing agent that may collaborate on overlapping work.</small></div>`);panel.appendChild(team)}
    if((draft.routine_suggestions||[]).length){const routines=document.createElement('section');routines.className='platform-section';routines.innerHTML='<div class="platform-section-title">Routine opportunities explicitly mentioned in your description</div><div class="platform-options"></div>';panel.appendChild(routines);draft.routine_suggestions.forEach((x,i)=>{const row=document.createElement('label');row.className='platform-option';row.innerHTML=`<input type="checkbox" data-routine-index="${i}" checked><div><strong>${esc(x.trigger)}</strong><small>${esc(x.action)}</small></div>`;routines.querySelector('.platform-options').appendChild(row)})}
    const approval=document.createElement('section');approval.className='platform-section';approval.innerHTML='<div class="platform-section-title">Approval boundaries</div><div class="platform-help">Reading, drafting and recommendations can be automatic. Sending, publishing, deleting/overwriting, purchases/payments, transfers, permission changes, production changes and accepting legal terms require approval by default.</div>';panel.appendChild(approval);
    const actions=document.createElement('div');actions.className='platform-actions';actions.innerHTML='<button type="button" data-cancel>Cancel</button><button type="button" class="primary" data-create>Create agent</button>';panel.appendChild(actions);const status=document.createElement('div');status.className='platform-status';panel.appendChild(status);actions.querySelector('[data-cancel]').onclick=close;actions.querySelector('[data-create]').onclick=async()=>{const selected=[...panel.querySelectorAll('[data-option-id]:checked')],routineIndexes=[...panel.querySelectorAll('[data-routine-index]:checked')].map(x=>Number(x.dataset.routineIndex)),payload={...draft,name:grid.querySelector('[data-name]').value.trim(),job:grid.querySelector('[data-job]').value.trim(),goal:grid.querySelector('[data-goal]').value.trim(),working_style:grid.querySelector('[data-style]').value.trim(),responsibilities:grid.querySelector('[data-responsibilities]').value.split('\n').map(x=>x.trim()).filter(Boolean),instructions:grid.querySelector('[data-instructions]').value.trim(),manager_id:grid.querySelector('[data-manager]').value||null,can_delegate:grid.querySelector('[data-delegate]').checked,skills:selected.filter(x=>x.dataset.optionKind==='skill').map(x=>x.dataset.optionId),plugins:selected.filter(x=>x.dataset.optionKind==='plugin').map(x=>x.dataset.optionId)};if(!payload.name||!payload.job){status.textContent='Name and job are required.';return}const b=actions.querySelector('[data-create]');b.disabled=true;status.textContent='Creating…';try{const d=await jsonFetch('/agent-builder/create',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});if(typeof window.loadPersistentAgents==='function')await window.loadPersistentAgents();for(const i of routineIndexes){const r=(draft.routine_suggestions||[])[i];if(!r)continue;await agentRun(`Create a routine called ${r.name} that ${r.trigger} ${r.action} for agent ${d.agent.name}`,d.agent)}status.textContent=d.connection_needed?.length?`Created ${d.agent.name}. Connect ${d.connection_needed.join(', ')} from Plugins when ready.`:`Created ${d.agent.name}.`;setTimeout(close,700)}catch(e){status.textContent=e.message;b.disabled=false}}
  }
  document.addEventListener('click',e=>{const b=e.target.closest?.('.agent-create');if(!b)return;e.preventDefault();e.stopImmediatePropagation();openBuilder()},true);
  async function openSkillBuilder(){const{panel,close}=modal('Create reusable Skill');const grid=document.createElement('div');grid.className='platform-grid';grid.innerHTML=`<div class="platform-field"><label>Name</label><input data-name></div><div class="platform-field"><label>Status</label><select data-status><option value="draft">Draft</option><option value="active">Active</option></select></div><div class="platform-field full"><label>When should an agent use it?</label><textarea data-when></textarea></div><div class="platform-field full"><label>Steps · one per line</label><textarea data-steps></textarea></div><div class="platform-field full"><label>Required inputs · one per line</label><textarea data-inputs></textarea></div><div class="platform-field full"><label>Required tools/plugins/access · one per line</label><textarea data-access></textarea></div><div class="platform-field full"><label>Decision rules · one per line</label><textarea data-rules></textarea></div><div class="platform-field full"><label>Expected output</label><textarea data-output></textarea></div><div class="platform-field full"><label>Validation rules · one per line</label><textarea data-validation></textarea></div><div class="platform-field full"><label>Approval boundaries · one per line</label><textarea data-approval>Use Agentie's normal approval path for consequential actions.</textarea></div><div class="platform-field full"><label>Failure handling</label><textarea data-failure>Stop on a real failure, explain what failed, and ask for missing input or permission instead of pretending it completed.</textarea></div>`;panel.appendChild(grid);const actions=document.createElement('div');actions.className='platform-actions';actions.innerHTML='<button type="button" class="primary" data-create>Create Skill</button>';panel.appendChild(actions);const status=document.createElement('div');status.className='platform-status';panel.appendChild(status);const lines=s=>s.split('\n').map(x=>x.trim()).filter(Boolean);actions.querySelector('[data-create]').onclick=async()=>{const payload={name:grid.querySelector('[data-name]').value.trim(),when_to_use:grid.querySelector('[data-when]').value.trim(),steps:lines(grid.querySelector('[data-steps]').value),required_inputs:lines(grid.querySelector('[data-inputs]').value),required_access:lines(grid.querySelector('[data-access]').value),decision_rules:lines(grid.querySelector('[data-rules]').value),expected_output:grid.querySelector('[data-output]').value.trim(),validation_rules:lines(grid.querySelector('[data-validation]').value),approval_boundaries:lines(grid.querySelector('[data-approval]').value),failure_handling:grid.querySelector('[data-failure]').value.trim(),status:grid.querySelector('[data-status]').value};try{const d=await jsonFetch('/workflow-skills',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});status.textContent=`Created ${d.name}. ${payload.status==='active'?'Assign it to an agent, then use Run skill '+d.name+'.':'Activate it when the workflow is ready.'}`;setTimeout(close,900)}catch(e){status.textContent=e.message}}}
  function addSkillButton(){const head=document.querySelector('#agentiePluginsPanel .plugins-head');if(!head||head.querySelector('.platform-skill-new'))return;const b=document.createElement('button');b.type='button';b.className='platform-skill-new';b.textContent='New Skill';b.onclick=e=>{e.stopPropagation();openSkillBuilder()};head.insertBefore(b,head.querySelector('.plugins-close'))}

  async function renderOwnedRoutines(){const panel=document.querySelector('.right-panel'),list=panel?.querySelector('.routine-list'),empty=panel?.querySelector('.routine-empty');if(!panel||!list||!document.querySelector('.app-shell')?.classList.contains('right-open'))return;const agent=activeAgent();try{const d=await jsonFetch(`/routines${agent?`?agent_id=${encodeURIComponent(agent.id)}`:''}`);list.replaceChildren();if(d.items.length){empty.style.display='none';for(const x of d.items){const row=document.createElement('div');row.className='routine-row';const s=document.createElement('strong');s.textContent=x.name||x.id;const m=document.createElement('small');m.textContent=[x.trigger,x.skill_id?`Skill: ${x.skill_id}`:'',x.status,x.owner_agent_name||'Unassigned'].filter(Boolean).join(' · ');row.append(s,m);list.appendChild(row)}}else{empty.style.display='grid'}}catch(_){}}
  document.addEventListener('click',e=>{const b=e.target.closest?.('.routine-create');if(!b)return;const agent=activeAgent();if(!agent)return;e.preventDefault();e.stopImmediatePropagation();const name=prompt('Routine name');if(!name?.trim())return;const action=prompt(`What should ${agent.name} do?`);if(!action?.trim())return;const trigger=prompt('When should it run? e.g. weekdays at 9 AM, or say: when a skill run completes','daily at 9 AM');if(!trigger?.trim())return;const text=`Create a routine called ${name.trim()} that ${trigger.trim()} ${action.trim()} for agent ${agent.name}`;agentRun(text,agent).then(()=>renderOwnedRoutines())},true);
  document.addEventListener('click',e=>{if(!e.target.closest?.('.topbar-button'))return;setTimeout(renderOwnedRoutines,180)},true);

  async function openChats(){const{panel}=modal('Agent chats');const head=document.createElement('div');head.className='platform-actions';head.innerHTML='<button type="button" class="primary" data-new>New group chat</button>';panel.appendChild(head);const list=document.createElement('div');list.className='platform-thread-list';panel.appendChild(list);async function load(){const d=await jsonFetch('/agent-threads');list.replaceChildren();(d.items||[]).forEach(t=>{const r=document.createElement('div');r.className='platform-thread-row';r.innerHTML=`<strong>${esc(t.name)}</strong><small>${esc((t.participants||[]).join(', '))} · ${Number(t.message_count||0)} messages</small>`;r.onclick=()=>openThread(t.id);list.appendChild(r)});if(!(d.items||[]).length)list.innerHTML='<div class="platform-help">No group chats yet.</div>'}head.querySelector('[data-new]').onclick=()=>newThread(load);load()}
  function newThread(after){const{panel,close}=modal('New group chat');const f=document.createElement('div');f.className='platform-field';f.innerHTML='<label>Name</label><input data-name>';panel.appendChild(f);const opts=document.createElement('div');opts.className='platform-options';(window.__agentieAgents||[]).forEach(a=>{const row=document.createElement('label');row.className='platform-option';row.innerHTML=`<input type="checkbox" value="${esc(a.id)}"><div><strong>${esc(a.name)}</strong><small>${esc(a.role||'')}</small></div>`;opts.appendChild(row)});panel.appendChild(opts);const actions=document.createElement('div');actions.className='platform-actions';actions.innerHTML='<button class="primary" type="button">Create chat</button>';panel.appendChild(actions);actions.firstChild.onclick=async()=>{const participants=[...opts.querySelectorAll('input:checked')].map(x=>x.value),name=f.querySelector('input').value.trim();if(!name||!participants.length)return;await jsonFetch('/agent-threads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,participants})});close();after?.()}}
  async function openThread(id){const{panel}=modal('Agent chat');const body=document.createElement('div');panel.appendChild(body);async function load(){const d=await jsonFetch(`/agent-threads/${encodeURIComponent(id)}`);panel.querySelector('.platform-head h3').textContent=d.name;body.replaceChildren();const meta=document.createElement('div');meta.className='platform-help';meta.textContent=`${(d.participants||[]).join(' · ')} · Use @Name to involve one or several agents in the same task.`;body.appendChild(meta);for(const m of d.messages||[]){const row=document.createElement('div');row.className='platform-thread-message';const job=m.job,jobText=job?` · job ${job.status}${job.final_output?' · completed result available':''}`:'';row.innerHTML=`<small>${esc(m.sender_name||m.sender_type)}${esc(jobText)}</small>${esc(m.message||'')}`;if(job?.final_output&&m.sender_type!=='agent'){const result=document.createElement('div');result.className='platform-help';result.style.marginTop='6px';result.textContent=job.final_output;row.appendChild(result)}body.appendChild(row)}const compose=document.createElement('div');compose.className='platform-grid';compose.innerHTML=`<div class="platform-field"><label>Send to</label><select data-target><option value="">Thread / @mentions</option>${(d.participant_ids||[]).map((aid,i)=>`<option value="${esc(aid)}">${esc((d.participants||[])[i]||aid)}</option>`).join('')}</select></div><div class="platform-field full"><label>Message / task — @mention multiple agents to run in parallel</label><textarea data-message placeholder="@Ben @Mira compare these options and report back"></textarea></div>`;body.appendChild(compose);const actions=document.createElement('div');actions.className='platform-actions';actions.innerHTML='<button type="button" class="primary">Send</button>';body.appendChild(actions);actions.firstChild.onclick=async()=>{const message=compose.querySelector('[data-message]').value.trim(),target=compose.querySelector('[data-target]').value;if(!message)return;await jsonFetch(`/agent-threads/${encodeURIComponent(id)}/messages`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message,target_agent_id:target||null})});await load()}}await load();const refresh=setInterval(()=>{if(panel.isConnected)load();else clearInterval(refresh)},2500)}
  async function showActivity(){const agent=activeAgent();try{const d=await agentRun(agent?`Show activity for ${agent.name}`:'Show activity timeline',agent);showRun(d)}catch(e){showRun({message:e.message,card:null})}}
  async function showSkills(){try{const d=await agentRun('Show workflow skills');showRun(d)}catch(e){showRun({message:e.message,card:null})}}
  function addPlatformButtons(){const sidebar=document.querySelector('.sidebar'),search=sidebar?.querySelector('.agent-search');if(!sidebar||!search)return;let anchor=search;if(!sidebar.querySelector('.platform-chats-launch')){const b=document.createElement('button');b.type='button';b.className='platform-chats-launch';b.textContent='💬 Agent chats';b.onclick=openChats;anchor.after(b);anchor=b}if(!sidebar.querySelector('.platform-skills-launch')){const b=document.createElement('button');b.type='button';b.className='platform-skills-launch';b.textContent='⚡ Reusable Skills';b.onclick=showSkills;anchor.after(b);anchor=b}if(!sidebar.querySelector('.platform-activity-launch')){const b=document.createElement('button');b.type='button';b.className='platform-activity-launch';b.textContent='◷ Activity';b.onclick=showActivity;anchor.after(b)}}
  let queued=false;new MutationObserver(()=>{if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;addSkillButton();addPlatformButtons()})}).observe(document.body,{childList:true,subtree:true});setTimeout(()=>{addSkillButton();addPlatformButtons()},120);
})();

(()=>{
  if(window.__agentiePlatformLayerLoader)return;window.__agentiePlatformLayerLoader=true;
  const layers=['/platform-automation.js?v=1','/platform-permission-guard.js?v=1','/platform-next4.js?v=1'];
  const load=index=>{if(index>=layers.length)return;const script=document.createElement('script');script.src=layers[index];script.async=false;script.onload=()=>load(index+1);script.onerror=()=>console.error('Agentie platform layer failed to load:',layers[index]);document.head.appendChild(script)};
  load(0);
})();